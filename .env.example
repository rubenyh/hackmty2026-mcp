# PostgreSQL URL for a dedicated read-only role. Direct and session-pooler URLs are supported.
# Keep TLS enabled. Never use the Supabase postgres role or put a service-role API key here.
SUPABASE_DATABASE_URL=postgresql://mcp_reader:REPLACE_WITH_PASSWORD@db.PROJECT_REF.supabase.co:5432/postgres?sslmode=require
# Session-pooler example (copy the exact region, project reference, and port from Supabase):
# SUPABASE_DATABASE_URL=postgresql+psycopg://mcp_reader.PROJECT_REF:REPLACE_WITH_PASSWORD@aws-0-REGION.pooler.supabase.com:5432/postgres?sslmode=require

# Comma-separated PostgreSQL schemas. At least one valid schema is required.
MCP_ALLOWED_SCHEMAS=public
# Exact comma-separated tables/views. Empty means deny all. Unqualified names are allowed only
# when MCP_ALLOWED_SCHEMAS contains exactly one schema.
MCP_ALLOWED_TABLES=public.users,public.accessibility_preferences,public.accounts,public.account_details,public.cards,public.credit_card_terms,public.transactions,public.beneficiaries,public.payment_orders,public.budgets,public.savings_goals,public.savings_contributions,public.scheduled_cash_flows,public.subscriptions,public.transfers,public.monthly_cash_flow

# Row limits: both must be positive, default must not exceed max, and max is capped at 10000.
MCP_DEFAULT_LIMIT=50
MCP_MAX_LIMIT=200
# Per-transaction PostgreSQL statement timeout in milliseconds (100-60000).
MCP_STATEMENT_TIMEOUT_MS=5000

# Optional stateless prediction service. Configure URL and key together.
# This bearer credential is server-only and is never forwarded from a user session.
INFERENCE_API_URL=http://127.0.0.1:8001
INFERENCE_API_KEY=hola
INFERENCE_HTTP_TIMEOUT_SECONDS=30
INFERENCE_CONNECT_TIMEOUT_SECONDS=5

# Transport is stdio or http. Host and port are used by the http transport.
MCP_TRANSPORT=stdio
MCP_HOST=127.0.0.1
MCP_PORT=8000

# DEBUG, INFO, WARNING, ERROR, or CRITICAL.
LOG_LEVEL=INFO

# Optional user-confirmed financial writes. Apply both checked-in action migrations first.
# Dedicated role only; keep SUPABASE_DATABASE_URL read-only.
# MCP_ACTIONS_DATABASE_URL=postgresql://fluidbank_actions:CHANGE_ME@HOST:5432/postgres?sslmode=require

# Same random secret on agent and MCP; at least 32 characters, never public.
# MCP_ACTIONS_SECRET=REPLACE_WITH_A_RANDOM_SERVER_ONLY_SECRET
